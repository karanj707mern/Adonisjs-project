---
import Layout from './Layout.astro'

interface Props {
  title?: string
  description?: string
  hideSiteChrome?: boolean
}

const { title, description, hideSiteChrome = false } = Astro.props
---
<Layout title={title} description={description} hideSiteChrome={hideSiteChrome}>
  <slot />
</Layout>
